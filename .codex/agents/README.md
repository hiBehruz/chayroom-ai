# Codex Agents

This directory is the Codex-compatible home for project agent definitions.

No `.claude/agents/` directory exists in this checkout, so there are currently no Claude agents to convert. If project-local Claude agents are added later, convert each important `.md` file into a `.toml` file here and place the original instructions in `developer_instructions`.
