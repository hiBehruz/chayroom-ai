# Minimal project-local Codex configuration.
# Keep secrets, API keys, and machine-specific overrides out of this file.

model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
approval_policy = "on-request"
