# Production .env for the self-hosted (Docker Compose) deploy.
# Copy this file to `.env` on the server and fill in real values.
# NEVER commit real secrets.

# --- Database (Docker Postgres) ---
# Only set the password here. docker-compose builds DATABASE_URL automatically as
#   postgres://chayroom:${POSTGRES_PASSWORD}@postgres:5432/chayroom
# and injects it into the nuxt + migrate containers, so you do NOT set DATABASE_URL.
POSTGRES_PASSWORD=

# --- Cache ---
# REDIS_URL is wired automatically by docker-compose (redis://redis:6379). Leave unset.

# --- App ---
NUXT_PUBLIC_APP_URL=https://chayroom.uz
NUXT_PUBLIC_SUPPORT_USERNAME=

# --- Telegram auth/bot ---
NUXT_PUBLIC_TELEGRAM_BOT_USERNAME=
NUXT_TELEGRAM_BOT_TOKEN=
# Secret for the bot webhook (X-Telegram-Bot-Api-Secret-Token). Generate as above.
NUXT_TELEGRAM_WEBHOOK_SECRET=

# --- OpenAI ---
NUXT_OPENAI_API_KEY=

# --- S3-compatible media storage ---
# Generic settings take precedence. They support MinIO and other S3 providers.
NUXT_STORAGE_ENDPOINT=
NUXT_STORAGE_REGION=
NUXT_STORAGE_BUCKET=
NUXT_STORAGE_PUBLIC_URL=
NUXT_STORAGE_ACCESS_KEY_ID=
NUXT_STORAGE_SECRET_ACCESS_KEY=
NUXT_STORAGE_FORCE_PATH_STYLE=false

# Legacy Cloudflare R2 fallback. Keep these until production storage is migrated.
NUXT_R2_ACCOUNT_ID=
NUXT_R2_ACCESS_KEY_ID=
NUXT_R2_SECRET_ACCESS_KEY=
NUXT_R2_BUCKET_NAME=
NUXT_R2_PUBLIC_URL=

# --- Sentry ---
NUXT_PUBLIC_SENTRY_DSN=

# --- Tribute (payments) ---
NUXT_TRIBUTE_API_KEY=
# Tribute checkout link (Telegram "Поделитесь ссылкой" → Telegram ссылка)
NUXT_PUBLIC_TRIBUTE_URL=https://t.me/tribute/app?startapp=sXgF

# --- Admin authorization ---
# Comma-separated numeric Telegram ids allowed into the admin panel.
NUXT_ADMIN_TELEGRAM_IDS=
# Random secret, >= 32 chars. Generate with:
#   node -e "console.log(require('node:crypto').randomBytes(24).toString('base64url'))"
NUXT_ADMIN_SESSION_PASSWORD=
# Share the admin session cookie across apex + subdomains.
NUXT_ADMIN_COOKIE_DOMAIN=.chayroom.uz
# Signs the logged-in user session cookie (cx-session). >= 32 chars, generate as above.
NUXT_SESSION_PASSWORD=
